# depression-ai

Project skeleton for the depression-ai project.

Structure:
- data/
- model/
- ontology/
- backend/
- frontend/

Run `python main.py` to run the starter entrypoint.

# Use the command on root level to run the backend
 uvicorn backend.app:app --reload --host 127.0.0.1 --port 8000